<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.matheusm.help2gether">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastroActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".InstituicaoActivity" />
        <activity android:name=".EventoActivity" />
        <activity android:name=".AvaliacaoActivity" />
        <activity android:name=".FaleConoscoActivity" />
        <activity android:name=".Instituicao1Activity" />
        <activity android:name=".Instituicao2Activity" />
        <activity android:name=".Evento1Activity" />
        <activity android:name=".Evento2Activity"></activity>
    </application>

</manifest>